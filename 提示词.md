  你是一名具备 Web 调试、协议分析、浏览器自动化和 Python 开发能力的高级工程师。
  请帮助我从零开发一个图书馆座位自动预约脚本。

  ## 项目目标

  分析目标图书馆预约系统的正常使用流程，开发一个可以由普通用户在本地运行的自动抢
  座程序。

  程序需要支持：

  - 用户登录；
  - 登录状态维护；
  - 指定预约日期；
  - 指定图书馆、楼层或区域；
  - 指定目标座位；
  - 查询座位状态；
  - 在预约开放后自动尝试预约；
  - 判断预约是否成功；
  - 输出清晰的运行结果；
  - 在网络异常或登录失效时进行合理处理。

  最终目标是让用户能够完成以下操作：

  ```text
  安装依赖
  → 配置账号和目标座位
  → 保存必要凭证
  → 执行命令
  → 程序等待预约开放
  → 自动登录或恢复会话
  → 查询目标座位
  → 尝试预约
  → 返回明确结果

  开发要求

  请从零设计并实现完整项目，不假设当前目录中已经存在可复用代码。

  项目应至少包含以下能力：

  1. 登录模块；
  2. HTTP 请求或浏览器自动化模块；
  3. Cookie、Token 或登录会话管理；
  4. 座位信息查询模块；
  5. 预约模块；
  6. 定时执行模块；
  7. 配置管理模块；
  8. 命令行入口；
  9. 日志和错误提示；
  10. 基本测试或验证脚本；
  11. 使用说明。

  你可以根据目标系统的实际情况，自主选择最合适的实现方式：

  - 使用 HTTP 请求直接调用接口；
  - 使用 Playwright、Selenium 等浏览器自动化工具；
  - 使用浏览器自动化完成登录，再复用会话调用接口；
  - 组合使用多种方式。

  应优先选择稳定、易维护、请求量较低、最接近正常用户行为的方案。

  功能要求

  账号管理

  支持通过配置文件或环境变量配置：

  - 账号标识；
  - 用户名；
  - 预约区域；
  - 目标座位；
  - 预约日期；
  - 预约开放时间；
  - 其他目标系统要求的参数。

  密码不得硬编码在源代码中。应优先使用：

  - 操作系统凭证管理器；
  - 环境变量；
  - 交互式输入；
  - 其他安全的本地凭证存储方式。

  支持多账号时，不同账号的配置、Cookie、Token 和运行状态必须相互隔离。

  登录和会话

  程序应能够：

  - 完成目标系统要求的登录流程；
  - 保存和恢复必要的登录状态；
  - 判断当前登录状态是否有效；
  - 在登录过期后重新登录；
  - 正确处理登录失败；
  - 处理重定向、动态参数、Token、Cookie、验证码或二次验证等实际情况。

  如果系统要求用户人工完成验证码或二次验证，应保留人工完成环节，不得尝试绕过。

  座位查询

  程序应能够：

  - 查询指定日期的座位；
  - 查询指定区域或场馆；
  - 根据座位编号精确匹配目标座位；
  - 判断目标座位是否可预约；
  - 区分可用、已占用、不可用、已预约和未知状态；
  - 在日期尚未开放或参数错误时给出明确提示。

  自动预约

  程序应能够：

  - 在指定时间运行；
  - 支持立即执行的测试模式；
  - 在提交前确认目标日期、区域和座位；
  - 对目标座位进行预约；
  - 识别预约成功；
  - 识别座位已被占用；
  - 识别重复预约；
  - 识别尚未开放；
  - 识别登录失效；
  - 识别参数错误；
  - 识别网络异常和服务端异常；
  - 在预约请求超时或结果不明确时，先确认最终预约状态，避免盲目重复提交。

  网络和错误处理

  程序应具备：

  - 合理的连接超时；
  - 有限次数的重试；
  - 适当的退避等待；
  - 网络错误与业务错误区分；
  - 清晰的错误信息；
  - 可定位问题的调试信息；
  - 对服务端响应结构变化的提示。

  对于具有副作用的预约请求，不得因为普通超时就无条件重复提交。

  命令行

  至少提供类似以下的运行方式：

  python main.py --account <账号>
  python main.py --account <账号> --now
  python main.py --account <账号> --date <日期>
  python main.py --account <账号> --dry-run

  具体参数可以根据实际设计调整，但必须提供：

  - 正式运行模式；
  - 立即执行测试模式；
  - 不提交预约的测试或诊断模式；
  - 指定账号的方式；
  - 指定日期、区域或座位的方式；
  - 帮助信息。

  安全要求

  必须遵守以下安全边界：

  1. 只使用用户本人或明确授权的账号；
  2. 不访问其他用户的数据；
  3. 不绕过验证码、短信验证、二次认证、权限控制或风控机制；
  4. 不进行接口扫描、压力测试、高频请求或并发轰击；
  5. 不把密码、Token、Cookie 或完整身份信息写入源码；
  6. 不在日志中打印敏感凭证；
  7. 配置示例中只能使用虚假数据；
  8. 遵守目标系统的使用规则和访问频率限制。

  代码和项目要求

  请创建一个完整的、可直接运行的项目，至少包括：

  - 清晰的目录结构；
  - 源代码；
  - 依赖文件；
  - 配置示例；
  - 忽略敏感文件的 .gitignore；
  - 使用说明；
  - 基本测试；
  - 运行示例；
  - 故障排查说明。

  代码应：

  - 使用 Python；
  - 保持模块职责清晰；
  - 便于替换接口参数和页面结构；
  - 避免把所有逻辑堆积在一个文件中；
  - 不引入没有必要的复杂框架；
  - 对关键失败场景提供可理解的错误提示。

  验收标准

  最终项目必须能够说明并尽可能验证以下内容：

  1. 如何安装和启动；
  2. 如何配置账号和座位；
  3. 如何安全地提供密码；
  4. 如何完成首次登录；
  5. 如何恢复已有登录状态；
  6. 如何查询目标座位；
  7. 如何执行测试模式；
  8. 如何执行正式预约；
  9. 如何判断预约成功；
  10. 预约失败时如何处理；
  11. 预约请求超时后如何避免重复预约；
  12. 如何处理登录过期；
  13. 如何更新发生变化的接口或页面参数；
  14. 哪些功能已经实际验证；
  15. 哪些功能依赖目标系统当前实现，未来可能失效。

  最终交付内容

  请最终提供：

  1. 完整项目代码；
  2. 项目目录结构；
  3. 依赖安装命令；
  4. 配置文件示例；
  5. 启动命令；
  6. 测试命令；
  7. 登录、查询和预约流程说明；
  8. 错误处理和重试策略；
  9. 凭证保护说明；
  10. 测试结果；
  11. 已知限制；
  12. 常见问题排查方法。

  不要声称一个未经实际验证的接口或流程已经稳定可用。对于无法确认的部分，请明确说
  明需要用户提供哪些页面信息、浏览器请求记录或测试结果。